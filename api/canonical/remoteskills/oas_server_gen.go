// Code generated by ogen, DO NOT EDIT.

package remoteskills

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// CreateRemoteSkillTarget implements create_remote_skill_target operation.
	//
	// Create a pending project target and return one short-lived enrollment code exactly once.
	//
	// POST /v1/skill/remote/target/create
	CreateRemoteSkillTarget(ctx context.Context, req *CreateRemoteSkillTargetRequest) (CreateRemoteSkillTargetRes, error)
	// EnrollRemoteSkillTarget implements enroll_remote_skill_target operation.
	//
	// Consume one short-lived enrollment code and return a per-target credential exactly once.
	//
	// POST /v1/skill/remote/target/enroll
	EnrollRemoteSkillTarget(ctx context.Context, req *EnrollRemoteSkillTargetRequest) (EnrollRemoteSkillTargetRes, error)
	// ListRemoteSkillTargets implements list_remote_skill_targets operation.
	//
	// Return credential-free target metadata and desired/observed publication state for one scope.
	//
	// POST /v1/skill/remote/targets
	ListRemoteSkillTargets(ctx context.Context, req *ListRemoteSkillTargetsRequest) (ListRemoteSkillTargetsRes, error)
	// RenameRemoteSkillTarget implements rename_remote_skill_target operation.
	//
	// Change the human-readable target name with target generation CAS while retaining its durable
	// identity.
	//
	// POST /v1/skill/remote/target/rename
	RenameRemoteSkillTarget(ctx context.Context, req *RenameRemoteSkillTargetRequest) (RenameRemoteSkillTargetRes, error)
	// RevokeRemoteSkillTarget implements revoke_remote_skill_target operation.
	//
	// Revoke the per-target credential with target generation CAS while retaining durable identity.
	//
	// POST /v1/skill/remote/target/revoke
	RevokeRemoteSkillTarget(ctx context.Context, req *RevokeRemoteSkillTargetRequest) (RevokeRemoteSkillTargetRes, error)
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h   Handler
	sec SecurityHandler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, sec SecurityHandler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		sec:        sec,
		baseServer: s,
	}, nil
}
